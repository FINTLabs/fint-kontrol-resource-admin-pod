// types.ts
interface Data {
    Id: number,
    Feature: string,
    Operation: string,
    AccessRoleId: string
}

export default Data;
//TODO change data inteface to permissions and use that interface in the permissions tab
